import React from 'react';

const Footer = () => {
    return (
        <div style={{backgroundColor: 'red', width: '100%', height: '100vh'}}>
         HELLO
        </div>
    );
};

export default Footer;